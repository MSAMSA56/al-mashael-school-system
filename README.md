# نظام إدارة مدرسة المشاعل الأهلية
# Al-Mashael School Management System

نظام إدارة مدرسية شامل ومتكامل لمدرسة المشاعل الأهلية للبنين - يشمل أنظمة دخول متعددة للطلاب والمعلمين والإداريين مع واجهة مستخدم عربية حديثة وخادم Express متقدم.

## 🎯 المميزات الرئيسية

- ✅ **نظام مصادقة آمن** - تسجيل دخول وإنشاء حسابات بتشفير bcrypt و JWT
- ✅ **واجهة مستخدم عربية حديثة** - تصميم متجاوب يدعم الوضع الليلي
- ✅ **API RESTful متكامل** - خوادم Express مع معالجة أخطاء احترافية
- ✅ **قاعدة بيانات PostgreSQL** - تخزين آمن للبيانات مع علاقات منطقية
- ✅ **نظام صلاحيات متقدم** - تحكم دقيق بالوصول حسب دور المستخدم
- ✅ **دعم أدوار متعددة** - طالب، معلم، مسؤول
- ✅ **تصميم Full Stack** - فصل كامل بين Frontend و Backend

## 🛠️ المتطلبات

- **Node.js** >= 18.0.0
- **npm** >= 9.0.0
- **PostgreSQL** >= 12.0

## 📦 التثبيت والإعداد

### 1. استنساخ المشروع
```bash
git clone https://github.com/MSAMSA56/al-mashael-school-system.git
cd al-mashael-school-system
```

### 2. تثبيت المكتبات
```bash
npm install
```

### 3. إعداد متغيرات البيئة
```bash
# انسخ ملف .env.example إلى .env
cp .env.example .env

# ثم عدّل البيانات حسب بيئتك
nano .env
```

### 4. إنشاء قاعدة البيانات
```bash
npm run init-db
```

### 5. بدء الخادم
```bash
# للتطوير (مع إعادة تحميل تلقائية)
npm run dev

# للإنتاج
npm start
```

سيكون الخادم متاحاً على `http://localhost:3000`

## 📁 هيكل المشروع

```
al-mashael-school-system/
├── public/                 # ملفات Frontend الثابتة
│   ├── index.html         # صفحة تسجيل الدخول
│   ├── app.js             # سكريبت الواجهة الأمامية
│   └── style.css          # تنسيقات CSS
├── routes/                # مسارات API
│   ├── auth.js            # مسارات المصادقة
│   └── users.js           # مسارات إدارة المستخدمين
├── models/                # نماذج البيانات
│   └── User.js            # نموذج المستخدم
├── middleware/            # Middleware Express
│   └── authMiddleware.js  # Middleware المصادقة والتفويض
├── config/                # ملفات الإعدادات
│   ├── database.js        # إعدادات قاعدة البيانات
│   └── auth.js            # دوال المصادقة والتشفير
├── server.js              # ملف الخادم الرئيسي
├── init-db.js             # سكريبت إنشاء قاعدة البيانات
├── package.json           # المكتبات والمعلومات
├── .env                   # متغيرات البيئة
└── README.md              # هذا الملف
```

## 🔐 حسابات تجريبية

| البريد الإلكتروني | كلمة المرور | الدور |
|---|---|---|
| ahmed@almashaeel.edu.sa | password123 | طالب |
| khalid@almashaeel.edu.sa | password123 | معلم |
| admin@almashaeel.edu.sa | password123 | مسؤول |

## 🌐 API Endpoints

### المصادقة
```
POST   /api/auth/login       - تسجيل الدخول
POST   /api/auth/register    - إنشاء حساب جديد
GET    /api/auth/me          - بيانات المستخدم الحالي
POST   /api/auth/logout      - تسجيل الخروج
```

### المستخدمون
```
GET    /api/users            - جميع المستخدمين (مسؤول فقط)
GET    /api/users/:id        - بيانات مستخدم معين
PUT    /api/users/:id        - تحديث بيانات المستخدم
DELETE /api/users/:id        - حذف مستخدم (مسؤول فقط)
```

### صحة الخادم
```
GET    /api/health           - فحص صحة الخادم
```

## 📝 أمثلة الاستخدام

### تسجيل الدخول
```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "ahmed@almashaeel.edu.sa",
    "password": "password123"
  }'
```

### الحصول على بيانات المستخدم
```bash
curl -X GET http://localhost:3000/api/auth/me \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

## 🔒 الأمان

- ✅ تشفير كلمات المرور باستخدام bcryptjs
- ✅ مصادقة JWT آمنة
- ✅ Helmet لحماية headers HTTP
- ✅ CORS مُعدّ بشكل آمن
- ✅ التحقق من صحة المدخلات
- ✅ معالجة آمنة للأخطاء

## 🚀 النشر على الإنتاج

### متطلبات الإنتاج
1. تغيير `JWT_SECRET` في ملف `.env`
2. تعيين `NODE_ENV=production`
3. إعداد قاعدة بيانات PostgreSQL منفصلة
4. استخدام متغيرات البيئة الآمنة

### خيارات النشر
- **Heroku** - نشر سهل مع دعم PostgreSQL
- **DigitalOcean** - خوادم VPS موثوقة
- **AWS** - منصة سحابية شاملة
- **Manus** - منصة نشر متخصصة

## 📚 المكتبات المستخدمة

| المكتبة | الإصدار | الوصف |
|---|---|---|
| express | ^4.18.2 | إطار عمل ويب |
| pg | ^8.11.3 | درايفر PostgreSQL |
| bcryptjs | ^2.4.3 | تشفير كلمات المرور |
| jsonwebtoken | ^9.1.2 | إنشاء والتحقق من JWT |
| cors | ^2.8.5 | معالج CORS |
| helmet | ^7.1.0 | حماية HTTP headers |
| express-validator | ^7.0.0 | التحقق من صحة المدخلات |
| dotenv | ^16.3.1 | إدارة متغيرات البيئة |

## 🐛 حل المشاكل الشائعة

### خطأ الاتصال بقاعدة البيانات
```
تأكد من:
1. تشغيل PostgreSQL
2. بيانات الاتصال صحيحة في .env
3. قاعدة البيانات موجودة
```

### خطأ في تثبيت المكتبات
```bash
# احذف node_modules و package-lock.json
rm -rf node_modules package-lock.json

# أعد التثبيت
npm install
```

### الخادم لا يبدأ
```bash
# تحقق من استخدام المنفذ
lsof -i :3000

# استخدم منفذ مختلف
PORT=3001 npm start
```

## 📞 الدعم والمساعدة

للمساعدة والدعم الفني، يرجى التواصل عبر:
- البريد الإلكتروني: support@almashaeel.edu.sa
- الموقع: https://almashaeel.edu.sa
- الهاتف: +966-XX-XXXX-XXXX

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT. انظر ملف LICENSE للتفاصيل.

## 👨‍💻 المساهمون

- **فريق تطوير المشاعل** - التطوير والصيانة الأساسية
- **المجتمع** - التحسينات والإصلاحات

## 🔄 التحديثات والتحسينات المستقبلية

- [ ] إضافة نظام إدارة الدرجات المتقدم
- [ ] نظام الرسائل والإشعارات
- [ ] تطبيق الهاتف المحمول
- [ ] نظام الحضور والغياب الذكي
- [ ] لوحة معلومات متقدمة
- [ ] تقارير شاملة وتحليلات

---

**آخر تحديث:** نوفمبر 2025
**الإصدار:** 2.0.0 (Full Stack)
